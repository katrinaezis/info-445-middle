# Setup

Run `npm install` to fetch dependencies

# Running

Set the environment variable `PASSWORD`

Use: `export PASSWORD="supersecretpass"` on Mac \ Linux

# API

Everything should now be running on localhost:3000. Go to your browser and test it out.
